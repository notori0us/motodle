/**
 * Payload budget enforcement (§5.9).
 *
 * This is a PASSING STUB, shipped by W0 so that `npm run build` is green from the first
 * commit. W5 replaces the body with the real implementation: gzip every dist/ artifact,
 * compare against the limits in `schema/constants.ts`, print a table, and exit non-zero
 * on any breach. Nobody else touches this file (§8).
 */

console.log('budgets: stub (W5 replaces this)');
process.exit(0);
